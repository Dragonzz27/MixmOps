export async function streamChat(question,id,onToken,signal){const response=await fetch('/api/chatStream',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,id}),signal});if(!response.ok)throw new Error('对话请求失败');const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='';while(true){const {value,done}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:true});const chunks=buffer.split('\n\n');buffer=chunks.pop()||'';for(const chunk of chunks){const line=chunk.split('\n').find(x=>x.startsWith('data:'));if(!line)continue;const data=line.slice(5).trim();if(data.includes('[DONE]'))return;onToken(data.replace(/^data:\s*/,''))}}}
