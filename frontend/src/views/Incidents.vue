<template><div class="page"><div class="section-title"><div><h2>故障排查</h2><span class="muted">从系统告警进入人工 Agent 排查</span></div></div><div class="panel"><n-empty v-if="!items.length" description="暂无故障排查记录"/><n-list v-else hoverable><n-list-item v-for="i in items" :key="i.id"><div><strong>{{i.alert_name}}</strong><div class="muted">{{i.status}} · {{new Date(i.created_at).toLocaleString()}}</div></div></n-list-item></n-list></div></div></template>
<script setup>import {ref,onMounted} from 'vue';import {incidentApi} from '@/api';const items=ref([]);onMounted(async()=>{items.value=(await incidentApi.list()).incidents||[]})</script>
